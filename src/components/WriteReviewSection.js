import styles from "./WriteReviewSection.module.css";
import Header from "./Header"

export default function WriteReviewSection() {
    return (
        <section className={styles.WriteReviewSection}>
            <Header />
        </section>
    )
}
